# rnui.dev

## Agent skills

### Issue tracker

Local markdown — issues and specs live as files under `.scratch/<feature>/`, not in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical roles, each label string equal to its name, recorded as a `Status:` line in each issue file. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

## Implementing without a named ticket

`/implement` with no ticket named means **take the frontier** of the active effort. Do not ask which one, and do not re-plan — the spec is frozen.

Active effort: `.scratch/notify-and-preview/`. **The frontier has run out — there is no agent-takeable ticket left here.** 13 went `resolved` on 2026-08-16, and nothing it unblocked is takeable. 01, 02, 04, 12 and 13 are `resolved`; 03, 05, 06 and 07 are `ready-for-human`; 08, 09, 10 and 11 are `ready-for-agent` but every one is still blocked — 08 by 05, and 09 through 11 transitively. So `/implement` takes **nothing** in this effort and hands back to the maintainer; the effort moves again only when a human closes 03, 05, 06 or 07. Do not substitute a `ready-for-human` ticket to have something to do. A ticket is available only when its `Status:` is `ready-for-agent` **and every number on its `Blocked by:` line is `resolved`** — `ready-for-human` is not `resolved`, and reading it as "near enough" is how a ticket gets built on a decision nobody made. What still blocks the *email* half is 05, and it is not a DNS problem: the DKIM record published at `resend._domainkey.mail.rnui.dev` is byte-identical to what Resend asks for and returns the same hash from three public resolvers and both Cloudflare authoritative nameservers, **and that is now stale: measured again on 2026-09-25, `mail.rnui.dev` reads `verified` in Resend with sending enabled, and `_dmarc.rnui.dev` exists at `p=none`, which ticket 05 recorded as absent.** `submission-receipt` then sent three real messages from that domain and Resend records all three `delivered`, so the Resend-support step this paragraph named is spent. `p=none` is still worth knowing: Resend documents it as "allow all email" rather than protection. 03's bulk verifier run is done (Emailable, all 29, 2026-08-16); 03 stays `ready-for-human` only because its privacy-policy processor edit is committed but not yet deployed. **The Preview is live at `preview.rnui.dev`**, serving the branch build, noindexed, reporting into PostHog project 559028 — and Vercel Authentication is now **off** for the project, which is what made it publicly reachable on the hobby plan. Anything deployed here is public; re-enable protection before serving anything that is not. 02 is `resolved`: its deploy shipped on 2026-08-15, and its last bullet — the PostHog property migration — was closed **not-applicable** by the maintainer, who accepted the single legacy `entry_opened` row rather than migrate it. That decision accepts one historical event and does **not** reopen ADR-0008's naming; `entry_id` and `entry_opened` must still never be emitted. It was charted by `/wayfinder`, so its frozen artifact is `map.md` rather than a `spec.md` — the twelve decisions under **Settled at charting** bind exactly as a spec's Constraints would, and `research/` holds two reports (consent/deliverability, and email service selection) that are not to be redone.

**SENDING HOLD, set by the maintainer 2026-08-15 and LIFTED by the maintainer 2026-09-25.** It forbade every outbound message until the maintainer lifted it and every open issue in the effort was fixed, and it covered the staged Digest sends in ticket 10 (including the first one to the maintainer), `pnpm broadcast:test` in ticket 05, and the live form submit in ticket 06, all three of which transmit real email. The maintainer lifted it on 2026-09-25 in one instruction to finish the remaining work, so outbound mail is now permitted and the verification tickets in `.scratch/submission-receipt/` run for real rather than ending `ready-for-human`. Worth knowing for the other effort: the second half of the condition was never reached there, since `.scratch/notify-and-preview/` still has 03, 05, 06 and 07 at `ready-for-human`, so this lift is the maintainer's call rather than a blocker being cleared. A green DKIM check or a cleared blocker never lifted it; only the maintainer did. Full text in ticket 10.

`.scratch/studio-dark/` is still open but has **no agent-takeable work left**: 7 tickets are `resolved` and the remaining 8 are all `ready-for-human`, waiting on the maintainer rather than on an agent. Do not take one of these from `/implement`. Its `spec.md` Goals, Non-goals and Constraints still bind any code that touches the site, and its checkpoints still stop an agent.

**Building: `.scratch/public-submissions/`.** Charted 2026-09-25 by `/wayfinder`. Read its `map.md` — there is no `spec.md` — before touching `/submit`. Its **Destination**, **Settled at charting** (13 decisions) and **Out of scope** bind any code that goes near the form, and its **Out of scope** is emphatic: no auto-publishing into the catalogue, no server-side or edge transcoding, and no presigned direct-to-R2 upload path. Twelve tickets, **eight resolved** — 01–05, 07, 08 and 10 — and **09 is `ready-for-human`**. The frontier is **06 and 11**: 06 replaces the picked file with compressed bytes *before* the Turnstile widget (which must not move ahead of it), and 11 is the discovery link from the footer and the Contributors page. 12 waits on 09, and 09 waits on a person. **09 is built, unit-tested and dry-run but cannot be `resolved`:** it delivers an outbound email, and the SENDING HOLD above forbids transmitting, so it needs the maintainer to lift the hold and let one Submission through. **The pipeline is not live yet.** `/submit` and `/api/submit` both exist, and the endpoint's refusals are verified against real Siteverify with a live bucket at zero objects — but **no Vercel environment carries `TURNSTILE_SECRET_KEY`, `TURNSTILE_HOSTNAMES`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `CLOUDFLARE_ACCOUNT_ID` or `CLOUDFLARE_R2_TOKEN`**, so a deployed `/submit` refuses every visitor, and setting them needs a `VERCEL_TOKEN` that is not in the shell. Firestore rules are now deployed and verify **41/41** — note that `pnpm rules:verify` tests the *file* and cannot see what is live; `pnpm rules:deploy` is the only thing that syncs them, and on 2026-09-25 the live set was 40 days stale. The two facts that shape all of it are Vercel's **4.5 MB** request-body wall (which is what makes the 5 MB cap real) and the free-tier CPU budgets that put compression in the browser.

**Charted 2026-09-25: `.scratch/submission-receipt/`.** A deliberate reversal of Public Submissions **decision 8** ("no receipt"), redrawn twice by the maintainer's own answers: it is **three messages**. One when a Demo arrives, one when it is published with the link, and one when something went wrong, that last one hand typed every time because its text does not exist until somebody writes it. Two are templated and fire automatically (an HTTP request, and a local publish); the third has no template and is a tooling question rather than a copy one. Read its `map.md` before touching it. Fourteen tickets, **thirteen resolved and 08 `ready-for-human`**. The three commands are `pnpm submissions:open`, `pnpm submissions:notify` and `pnpm submissions:outcome`, and `add-recording` carries the last two as steps 10 and after. The two verification tickets ran for real: every message this pipeline sends is `delivered` in Resend's own record, the notice's links were fetched and answer 200, and the audience was checked directly and is unchanged. **08 is `ready-for-human` for a reason that is not the hold:** Turnstile will not issue a token to an automated browser, so no agent can drive the deployed form end to end. What is left is one submission and one look at a mailbox, both of which need a person. Still open from the research: `_dmarc.rnui.dev` is `p=none`, which Resend documents as "allow all email" rather than protection, and placement in a stranger's inbox cannot be determined without sending to a stranger.

1. Read that directory's `spec.md` first, or its `map.md`, for an effort charted by `/wayfinder`. Its Goals, Non-goals and Constraints, or a map's **Settled at charting** and **Out of scope**, are binding — in particular `api_host` stays `https://us.i.posthog.com`, Firebase not PostHog owns view and vote counts, `/products`, `?category=`, `view_count` and `vote_count` keep their public spelling, and the three stored browser keys `"bookmarkedItems"`, `"votedItems"` and `"viewedEntryIds"` keep their exact strings. Also read `CONTEXT.md` — the domain is **Recording** and **Contributor**, never Entry or author (ADR-0008).
2. Scan `issues/`. A ticket is available when its `Status:` is `ready-for-agent` and every number on its `Blocked by:` line is `resolved`. Lowest number wins. No `Blocked by:` line means nothing blocks it. **If `spec.md` names a ticket to take first, that beats the lowest number** — some orderings are about a data-collection clock rather than a dependency, and cannot be written as `Blocked by:` without lying.
3. Set that ticket's `Status:` to `claimed` and save before writing any code.
4. Implement only that ticket. Its `## Acceptance` is the definition of done.
5. On finish: append what you did under `## Comments`, and commit the code and the ticket together. Set `Status: resolved` only when every `## Acceptance` bullet is actually met. If one needs a deploy, a person's judgement, or data that does not exist yet, set `ready-for-human` instead and name what is left and who does it — `resolved` is terminal, and claiming it early is how the remainder gets lost.

Stop and hand back to the maintainer at the checkpoints listed in `spec.md`.

Finished efforts, not to be reopened: `.scratch/ui-ux-overhaul/` — all 14 tickets `resolved` as of 2026-07-31. Its one outstanding checkpoint, `posthog-expansion` ticket 09 steps 2 to 4, is discharged: the dashboard exists and the success criteria were agreed on 2026-08-01, before deploy A and before any redesign data.

Paused effort: `.scratch/posthog-expansion/`. Three tickets are `resolved`, five are `ready-for-human`, and two — 08 and 11 — are `needs-triage` and want a maintainer decision, not an agent. Their old blocker is spent: **deploy A shipped on 2026-08-15T00:45:37Z**, production `3d479be`, aliased to `rnui.dev`. What 02, 03, 04 and 09 now wait on is *traffic accumulating*, not a deploy. Re-read a ticket's own `Status:` before taking it. `.scratch/studio-dark/spec.md`'s Sequence is the authority on the order.

**Deploy A is not the merge of `feat/studio-dark`, and conflating them is a live hazard.** Deploy A was cut from `76651a3` and *deliberately held the Studio Dark commits back*; merging `feat/studio-dark` into `main` is **deploy B**, which `studio-dark`'s checkpoint 13 gates. An earlier version of this file described deploy A as "the merge of `feat/studio-dark` to `main`, which has not happened" — both halves were wrong, and an agent acting on it would ship deploy B past its gate while believing it was doing deploy A. Note also that `origin/main` is **not** an ancestor of `feat/studio-dark`: `main` carries a pnpm lockfile fix the branch never received, so deploy B is a real merge, not a fast-forward.
